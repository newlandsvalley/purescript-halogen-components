{
  "private": true,
  "scripts": {
    "build": "spago build",
    "button": "spago -x ./button.dhall bundle-app --main Examples.Button.Main --to examples/button/dist/app.js",
    "fileio": "spago -x ./fileio.dhall bundle-app --main Examples.Fileio.Main --to examples/fileio/dist/app.js",
    "multiple-select": "spago -x ./multiple-select.dhall bundle-app --main Examples.MultipleSelect.Main --to examples/multiple-select/dist/app.js",
    "player": "spago -x ./player.dhall bundle-app --main Examples.Player.Main --to examples/player/dist/app.js",
    "thumbnail-player": "spago -x ./thumbnail-player.dhall bundle-app --main Examples.ThumbnailPlayer.Main --to examples/thumbnail-player/dist/app.js"
  },
  "devDependencies": {
    "spago": "^0.20.9",
    "purescript": "^0.15.4",
    "webpack": "^3.6.0"
  }
}
