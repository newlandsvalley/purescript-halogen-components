{
  "private": true,
  "scripts": {
    "build": "spago build --package halogen-components",
    "button": "spago bundle --package button --outfile dist/app.js --bundle-type app --platform browser",
    "fileio": "spago bundle --package fileio --outfile dist/app.js --bundle-type app --platform browser",
    "multiple-select": "spago bundle --package multiple-select --outfile dist/app.js --bundle-type app --platform browser",
    "player": "spago bundle --package player --outfile dist/app.js --bundle-type app --platform browser",
    "thumbnail-player": "spago bundle --package thumbnail-player --outfile dist/app.js --bundle-type app --platform browser"
  },
  "devDependencies": {
    "purescript": "^0.15.15",
    "spago": "^0.93.42"
  },
  "dependencies": {
    "esbuild": "^0.25.0"
  }
}
